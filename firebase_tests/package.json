{
  "name": "apartment-management-firebase-tests",
  "description": "Local Firebase Security Rules tests",
  "private": true,
  "scripts": {
    "test": "node --test test/firestore.rules.test.js",
    "test:rules": "node --test test/firestore.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
